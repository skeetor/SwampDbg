<Grid x:Class="WpfDockingManager.DockingGroup"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:WpfDockingManager"
			 x:Name="dockingGroupControl"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">

	<Grid.Resources>
		<local:TabPositionDockConverter x:Key="TabPositionConverter"/>
		<local:TabItemRotation x:Key="TabItemRotation"/>
		<Style x:Key="CloseButtonStyle" TargetType="Button">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Width" Value="16"/>
			<Setter Property="Height" Value="16"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Grid>
							<Path x:Name="Cross" Stroke="Gray" StrokeThickness="2" 
								Data="M4,4 L13,13 M4,13 L13,4"/>
						</Grid>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="Cross" Property="Stroke" Value="Black"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Grid.Resources>

	<!-- <TextBlock Text="{Binding ElementName=dockingGroupControl, Path=TabPosition}"></TextBlock> -->

	<TabControl Name="_tabControl" TabStripPlacement="{Binding ElementName=dockingGroupControl, Converter={StaticResource TabPositionConverter}, Path=TabPosition}">
		<TabControl.Resources>

			<Style TargetType="{x:Type TabItem}">
				<Setter Property="HeaderTemplate">
					<Setter.Value>
						<DataTemplate>
							<ContentPresenter Content="{TemplateBinding Content}">
								<ContentPresenter.LayoutTransform>
									<RotateTransform Angle="{Binding ElementName=dockingGroupControl, Converter={StaticResource TabItemRotation}, Path=TabPosition}" />
								</ContentPresenter.LayoutTransform>
							</ContentPresenter>
						</DataTemplate>
					</Setter.Value>
				</Setter>
				<Setter Property="Padding" Value="3" />
			</Style>

		</TabControl.Resources>
		<TabControl.ItemContainerStyle>
			<Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="TabItem">
							<Grid>
								<Border x:Name="Border" 
										Background="{TemplateBinding Background}"
										BorderThickness="{TemplateBinding BorderThickness}"
										BorderBrush="{TemplateBinding BorderBrush}"
										CornerRadius="2"
										Margin="2,2,2,0">
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="Auto"/>
										</Grid.ColumnDefinitions>

										<ContentPresenter x:Name="ContentSite"
														Grid.Column="0"
														VerticalAlignment="Center"
														HorizontalAlignment="Center"
														ContentSource="Header"
														RecognizesAccessKey="True"
														Margin="5,2,5,2"/>

										<Button x:Name="CloseButton"
												Grid.Column="1"
												Style="{StaticResource CloseButtonStyle}"
												Margin="5,0,5,0"
												Command="{Binding CloseTabCommand, RelativeSource={RelativeSource AncestorType={x:Type local:DockingGroup}}}"
												CommandParameter="{Binding}"
												Click="OnCloseButtonEvent"/>
									</Grid>
								</Border>
							</Grid>
							<ControlTemplate.Triggers>
								<Trigger Property="IsSelected" Value="True">
									<Setter TargetName="Border" Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
									<Setter TargetName="Border" Property="BorderBrush" Value="DarkGray"/>
								</Trigger>
								<Trigger Property="IsSelected" Value="False">
									<Setter TargetName="Border" Property="Background" Value="LightGray"/>
									<Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
								</Trigger>
								<Trigger Property="IsMouseOver" Value="True">
									<Setter TargetName="Border" Property="Background" Value="#FFDDDDDD"/>
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
				<Setter Property="Width" Value="{Binding TabWidth, RelativeSource={RelativeSource AncestorType={x:Type local:DockingGroup}}}"/>
				<Setter Property="Height" Value="{Binding TabHeight, RelativeSource={RelativeSource AncestorType={x:Type local:DockingGroup}}}"/>
			</Style>
		</TabControl.ItemContainerStyle>
	</TabControl>

</Grid>
